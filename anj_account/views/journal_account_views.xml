<?xml version="1.0" encoding="UTF-8"?>

<odoo>
    <record id="account_journal_form_inherit" model="ir.ui.view">
        <field name="name">account.journal.form.inherit</field>
        <field name="model">account.journal</field>
        <field name="inherit_id" ref="account.view_account_journal_form"/>
        <field name="arch" type="xml">
            <field name="type" position="after">
                <field name="no_header" string="Pas d'entête"/>
                <field name="is_customer_journal" string="Pas de nom de facture dans le pdf"/>
                <field name="no_footer" string="Pas de pied de page"/>
            </field>
        </field>
    </record>
</odoo>